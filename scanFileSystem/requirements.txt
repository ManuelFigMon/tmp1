# scanFileSystem.py dependencies.
#
# CSV output needs NOTHING beyond the Python standard library.
# XLSX output needs one Excel engine; without either, the scan falls back to
# CSV with a warning. pandas/numpy are deliberately NOT used -- they were the
# source of an "Unable to import required dependency numpy" failure and bought
# nothing over the stdlib csv module.

# --- runtime (only needed for .xlsx output) ---
openpyxl==3.1.5
# xlsxwriter==3.2.9        # optional alternative Excel engine

# --- tests ---
pytest==9.1.1
